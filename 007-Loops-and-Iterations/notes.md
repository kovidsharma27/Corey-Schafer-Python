# Video 07 — Python Tutorials for Beginners 7: Loops and Iterations - For/While Loops

## Status

✅ Completed

## What I Learned

- Learned what loops are and why they are used.
- Learned about the two basic types of loops in Python: `for` loops and `while` loops.
- Learned how to use a `for` loop to iterate through a list.
- Learned how `break` is used to stop a loop at a particular condition.
- Learned how `continue` is used to skip an iteration and move to the next value.
- Learned how nested loops work.
- Learned how a loop inside another loop executes for each value of the outer loop.
- Learned how to use `range()` to loop a specific number of times.
- Learned how to use `range(start, end)` to specify a starting value.
- Learned how the ending value in `range()` is not included.
- Learned how to use `range(start, end, step)` to control the increment or decrement.
- Learned how to use negative steps with `range()` to decrease values.
- Learned how `while` loops execute as long as their condition is `True`.
- Learned how to use `break` in a `while` loop.
- Learned how to use `continue` in loops.
- Learned how `while True` creates a loop that continues until it is stopped.
- Learned what an infinite loop is.
- Learned that `Ctrl + C` can be used to stop an accidentally running infinite loop.

## What I Practiced

- Using `for` loops
- Using `while` loops
- Iterating through lists
- Using `break`
- Using `continue`
- Creating nested loops
- Using the `range()` function
- Using different `range()` steps
- Increasing and decreasing loop values
- Using `while True`
- Stopping loops with `break`

## Verification

I ran the complete Video 07 code in VS Code and confirmed that it executes correctly and produces the expected output.

## Notes

This video introduced loops and iterations in Python.

I practiced using `for` and `while` loops, controlling loops with `break` and `continue`, creating nested loops, and using `range()` for repeated operations.

I also learned about infinite loops and how to stop one if it runs accidentally.

## Key Takeaway

I learned how to repeat operations efficiently using `for` and `while` loops and how to control their execution using `break`, `continue`, and loop conditions.
