# Video 02 — Strings, Variables, and String Methods

## Status

✅ Completed

## What I Learned

- Learned how to create and print variables.
- Learned that strings are represented by the `str` data type.
- Learned how to use single quotes and double quotes for strings.
- Learned how to use `\` to escape a quote inside a string.
- Learned how to create multi-line strings using `""" """` or `''' '''`.
- Learned how to continue a string onto another line using `\`.
- Learned how to find the length of a string using `len()`.
- Learned how to access individual characters in a string using indexing.
- Learned how to get part of a string using slicing.
- Learned how to convert strings to lowercase, uppercase, and title case.
- Learned how to count occurrences of a character or word using `.count()`.
- Learned how to find the position of a character or word using `.find()`.
- Learned how to replace part of a string using `.replace()`.
- Learned how to concatenate strings using `+`.
- Learned how to format strings using `.format()`.
- Learned how to use f-strings to insert variables directly into strings.
- Learned how to use string methods inside f-strings.
- Learned how to use `dir()` to see what can be done with a variable or object.
- Learned how to use `help()` to see documentation about Python objects and methods.

## What I Practiced

- Printing strings and variables
- Checking the type of a variable using `type()`
- String indexing and slicing
- String case conversion
- Counting and finding text
- Replacing text
- Concatenating strings
- Formatting strings
- Exploring Python's built-in string methods

## Verification

I ran the complete Video 02 code in VS Code and confirmed that it executes correctly and produces the expected output.

## Notes

This video introduced the basics of working with strings in Python.

I practiced different ways of creating, accessing, modifying, and formatting strings. I also learned how to explore the available methods of a Python object using `dir()` and `help()`.

## Key Takeaway

I learned the fundamentals of working with strings in Python, including indexing, slicing, string methods, concatenation, and formatting.
