# Video 03 — Numbers and Arithmetic

## Status

✅ Completed

## What I Learned

- Learned about integers (`int`) and floating-point numbers (`float`).
- Learned how to check the type of a variable using `type()`.
- Learned basic arithmetic operations using `+`, `-`, `*`, and `/`.
- Learned how integer division using `//` works.
- Learned how to calculate powers using `**`.
- Learned how to find the remainder using `%`.
- Learned about operator precedence and associativity.
- Learned how to increment a variable using `num = num + 1` and `num += 1`.
- Learned about augmented assignment operators such as `+=`, `-=`, `*=`, and `/=`.
- Learned how to find the absolute value using `abs()`.
- Learned how to round numbers using `round()`.
- Learned how to compare values using relational operators.
- Learned that comparisons return Boolean values: `True` or `False`.
- Learned how to convert a string to an integer using `int()`.

## What I Practiced

- Working with integers and floats
- Performing arithmetic operations
- Using floor division, exponentiation, and modulus
- Understanding operator precedence
- Using assignment and augmented assignment operators
- Finding absolute and rounded values
- Comparing numbers
- Converting strings to integers

## Verification

I ran the complete Video 03 code in VS Code and confirmed that it executes correctly and produces the expected output.

## Notes

This video introduced working with numbers in Python.

I practiced arithmetic operations, assignment operators, comparisons, rounding, absolute values, and converting strings into integers.

## Key Takeaway

I learned the fundamentals of working with numbers in Python and how to perform calculations, comparisons, and basic type conversion.
