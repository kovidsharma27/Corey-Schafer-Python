# Video 09 — Import Modules and Exploring the Standard Library

## Status

✅ Completed

## What I Learned

- Learned what Python modules are and why they are useful.
- Learned how to create and use a custom module.
- Learned how to import a module using `import module_name`.
- Learned how to use an imported module's functions and variables.
- Learned how to give an imported module an alias using `as`.
- Learned how to import specific functions or variables using `from module import ...`.
- Learned how to import multiple items from a module.
- Learned how to give imported functions an alias.
- Learned why `from module import *` can make code confusing.
- Learned about `sys.path` and the locations Python searches when importing modules.
- Learned how to temporarily add a location using `sys.path.append()`.
- Learned how to remove a location using `sys.path.remove()`.
- Learned about `PYTHONPATH` for adding directories to Python's module search path.
- Learned about Python's standard library.
- Practiced using the `random` module.
- Practiced using the `math` module.
- Practiced using the `datetime` and `calendar` modules.
- Practiced using the `os` module.
- Learned how to check the current working directory using `os.getcwd()`.
- Learned how to find the location of a module using `os.__file__`.
- Practiced a simple linear search function with `O(n)` time complexity and `O(1)` space complexity.

## What I Practiced

- Creating and importing custom modules
- Using different import methods
- Using module and function aliases
- Working with `sys.path`
- Using standard library modules
- Generating random values
- Performing mathematical operations
- Working with dates and leap years
- Checking directories and module locations

## Verification

I ran the Video 09 code in VS Code and confirmed that the examples execute correctly.

## Notes

This video introduced Python modules, importing code, and exploring the standard library.

I practiced creating my own module, importing functions and variables, working with Python's module search paths, and using several standard library modules.

## Key Takeaway

I learned how Python modules allow code to be organized and reused, and how Python's standard library provides many useful modules and functions that can be used without creating everything from scratch.
